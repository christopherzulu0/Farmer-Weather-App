{
  "version": 2,
  "installCommand": "npm install",
  "builds": [
    {
      "api": "package.json",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "npm run vercel-build"
      }
    }
  ],
  "routes": [
    {
      "api": "/(.*)",
      "dest": "app.js"
    }
  ]
}